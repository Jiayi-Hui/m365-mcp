# Tool index

Generated by `tools/dump_tool_index.py` - do not edit by hand.

142 tools.

## `m365_*` - server + application status, handles, file conversion

| tool | what it does |
| --- | --- |
| `m365_help` | Map of every tool this server exposes, grouped by application. |
| `m365_list_handles` | All live handles (workbooks, documents, presentations, publications). |
| `m365_quit_app` | Quit an Office application. Requires confirm=True - it can lose work. |
| `m365_release` | Release Office instances this server started, and verify they really died. |
| `m365_status` | Which Office apps are installed, which are running, and what is open. |

## `excel_*` - workbooks, sheets, ranges, formulas, formatting, charts, macros, plus model_map / trace_precedents / snapshot for financial models

| tool | what it does |
| --- | --- |
| `excel_activate` | Bring a workbook/sheet/cell to the front (useful before a screenshot). |
| `excel_add_chart` | Add a chart object from a data range. |
| `excel_add_sheet` | Add a worksheet, optionally named and positioned after an existing sheet. |
| `excel_append_rows` | Append rows below the last used row of `column` (table-style logging). |
| `excel_apply_changeset` | Apply a reviewed changeset to the workbook. Requires confirm=True. |
| `excel_autofit` | Autofit column widths and/or row heights. |
| `excel_calculate` | Recalculate. full=True forces a full rebuild; mode sets auto|manual. |
| `excel_cell_info` | Everything about one cell: value, formula, format, precedents, comment. |
| `excel_clear_range` | Clear a range. what: all | contents | formats | comments. |
| `excel_close` | Close a workbook. save=False discards unsaved changes. |
| `excel_copy_sheet` | Duplicate a worksheet inside the workbook, or copy it into another one. |
| `excel_data_sources` | Which live data add-ins does this workbook depend on, and are they loaded? |
| `excel_delete_sheet` | Delete a worksheet (no confirmation prompt - DisplayAlerts is off). |
| `excel_export_chart` | Export one chart to a PNG file. |
| `excel_export_pdf` | Export a workbook (or one sheet) to PDF. |
| `excel_find` | Find cells containing a value/formula text. look_in: values | formulas. |
| `excel_format_range` | Format a range. Colors are "#RRGGBB"; align is left|center|right. |
| `excel_insert_delete` | Insert or delete whole rows/columns. |
| `excel_list_charts` | List chart objects on a sheet (or the whole workbook). |
| `excel_list_names` | List defined names (named ranges) of a workbook. |
| `excel_list_open_handles` | Diagnostics: workbook handles this server has handed out. |
| `excel_list_sheets` | List worksheets with their used-range dimensions. |
| `excel_list_workbooks` | List the workbooks currently open in Excel, with handles to address them. |
| `excel_model_check` | Health check a model before trusting it with new data. |
| `excel_model_map` | Map a financial model: sheet roles, axes, and every assumption cell. |
| `excel_new` | Create a new, unsaved workbook (optionally from a template path). |
| `excel_open` | Open a workbook (or return the handle if it is already open). |
| `excel_propose_changes` | Validate a set of proposed model edits and write them to a changeset file. |
| `excel_read_range` | Read a range. Omit range_a1 to read the sheet's used range. |
| `excel_rename_sheet` | Rename a worksheet. |
| `excel_replace` | Find & replace inside a sheet (or a range). |
| `excel_run_macro` | Run a VBA macro/function by name (e.g. "Module1.Refresh" or "MyMacro"). |
| `excel_save` | Save a workbook. With `path`, saves a copy under the new name/format. |
| `excel_set_name` | Create/replace a defined name. refers_to looks like "=Sheet1!$A$1:$B$9". |
| `excel_snapshot` | Save a timestamped copy of a workbook before changing it. |
| `excel_trace_precedents` | Walk the precedent tree of a cell: what drives this number, recursively. |
| `excel_write_range` | Write a 2-D array (list of rows) starting at start_cell. |

## `word_*` - documents, text, outline, tables, images, comments, revisions

| tool | what it does |
| --- | --- |
| `word_add_comment` | Add a comment on a paragraph, or on the first match of anchor_text. |
| `word_append_text` | Append text at the end of the document, optionally with a style name. |
| `word_close` | Close a document (save=False discards changes). |
| `word_document_info` | Statistics and metadata: pages, words, headings, tracked-changes state. |
| `word_export_pdf` | Export the document to PDF. |
| `word_find` | Find occurrences and return their paragraph index plus surrounding text. |
| `word_format_paragraphs` | Format a paragraph range (1-based, inclusive). alignment: left|center|right|justify. |
| `word_get_outline` | List the heading structure (paragraph index, level, text). |
| `word_insert_break` | Insert a break. kind: page | section_next | column | line. |
| `word_insert_image` | Insert a picture at the end, or after a paragraph index. |
| `word_insert_paragraph` | Insert a new paragraph after the given 1-based paragraph index (0 = top). |
| `word_insert_table` | Insert a table from a 2-D list at the end (or after a paragraph index). |
| `word_insert_toc` | Insert a table of contents built from heading styles. |
| `word_list_comments` | List comments with author, anchor text and reply text. |
| `word_list_documents` | List documents open in Word. |
| `word_list_open_handles` | Diagnostics: document handles this server has handed out. |
| `word_list_styles` | List style names available in the document. |
| `word_list_tables` | List tables with their dimensions and first row. |
| `word_new` | Create a new document, optionally from a .dotx template. |
| `word_open` | Open a .docx/.doc/.rtf/.txt document (returns the existing handle if open). |
| `word_read_table` | Read a table into a 2-D list (1-based table index). |
| `word_read_text` | Read document text, optionally a paragraph slice (1-based, inclusive). |
| `word_replace_text` | Find & replace across the document body. |
| `word_revisions` | Tracked changes. action: list | enable | disable | accept_all | reject_all. |
| `word_run_macro` | Run a VBA macro in Word. |
| `word_save` | Save a document; with `path`, save-as (format inferred from extension). |

## `ppt_*` - presentations, slides, shapes, tables, charts, notes, export

| tool | what it does |
| --- | --- |
| `ppt_add_chart` | Add a native chart. `series` maps series name -> list of values. |
| `ppt_add_image` | Add a picture to a slide (omit width/height to keep the native size). |
| `ppt_add_slide` | Add a slide. layout: title|text|two_column|table|title_only|blank|object|chart|picture. |
| `ppt_add_table` | Add a table from a 2-D list. |
| `ppt_add_textbox` | Add a textbox at an absolute position (points; a 16:9 slide is 960x540). |
| `ppt_close` | Close a presentation (save=False discards changes). |
| `ppt_copy_slide_from` | Insert a slide from another presentation file (keeps its design). |
| `ppt_delete_shape` | Delete a shape from a slide. |
| `ppt_delete_slide` | Delete a slide by 1-based index. |
| `ppt_duplicate_slide` | Duplicate a slide; the copy lands right after the original. |
| `ppt_export_images` | Export slides as images (whole deck, or one slide). |
| `ppt_export_pdf` | Export the presentation to PDF. |
| `ppt_format_shape` | Move, resize, recolour a shape. |
| `ppt_list_open_handles` | Diagnostics: presentation handles this server has handed out. |
| `ppt_list_presentations` | List open presentations. |
| `ppt_list_shapes` | List the shapes on a slide with position, size and text. |
| `ppt_list_slides` | List slides with title, layout and (optionally) the text of every shape. |
| `ppt_move_slide` | Move a slide to another position. |
| `ppt_new` | Create a new presentation, optionally from a .potx/.pptx template. |
| `ppt_open` | Open a .pptx/.ppt/.potx presentation. |
| `ppt_replace_text` | Replace text across the deck (or one slide) - useful for templating. |
| `ppt_run_macro` | Run a VBA macro in PowerPoint. |
| `ppt_save` | Save a presentation; with `path`, save-as (format inferred from extension). |
| `ppt_set_notes` | Set the speaker notes of a slide. |
| `ppt_set_text` | Replace the text of a shape (by name or 1-based index). |

## `outlook_*` - mail, folders, search, drafts, calendar, contacts, tasks

| tool | what it does |
| --- | --- |
| `outlook_accounts` | List the mail accounts and stores available in this Outlook profile. |
| `outlook_calendar_list` | List calendar items in a window (default: now to +7 days). |
| `outlook_categories` | Master category list. action: list | add | remove. |
| `outlook_contacts_list` | List or search contacts. |
| `outlook_create_appointment` | Create a calendar appointment (or an unsent meeting request draft). |
| `outlook_create_draft` | Create an unsent draft and return its entry_id. Nothing is transmitted. |
| `outlook_create_task` | Create a task in the default Tasks folder. |
| `outlook_delete_appointment` | Delete a calendar item. Requires confirm=True. |
| `outlook_delete_draft` | Delete a draft you created (moves it to Deleted Items). |
| `outlook_delete_message` | Move a message to Deleted Items. Requires confirm=True. |
| `outlook_folder_stats` | Item/unread counts and the newest item of a folder. |
| `outlook_forward_draft` | Create a forward draft of an existing message (not sent). |
| `outlook_get_message` | Read one message in full. body_format: text | html. |
| `outlook_list_folders` | Walk the folder tree (of one store, or of every store). |
| `outlook_list_messages` | List messages in a folder with server-side filters. |
| `outlook_move_message` | Move a message to another folder. |
| `outlook_reply_draft` | Create a reply draft to an existing message (not sent). |
| `outlook_resolve_recipient` | Resolve a name against the address book (GAL) - check before sending. |
| `outlook_save_attachments` | Save a message's attachments to a folder. |
| `outlook_search` | Keyword search inside a folder (DASL contains-search, subfolders excluded). |
| `outlook_send_draft` | Send an existing draft. THIS TRANSMITS MAIL. |
| `outlook_tasks_list` | List tasks. |
| `outlook_update_message` | Mark read/unread, set categories, flag or change importance. |

## `onenote_*` - notebook hierarchy, page read/write, search, publish

| tool | what it does |
| --- | --- |
| `onenote_append_to_page` | Append one or more paragraphs to an existing page. |
| `onenote_create_page` | Create a page in a section; `content` may be a string or a list of lines. |
| `onenote_export` | Publish a page/section/notebook to a file. |
| `onenote_find_pages` | Full-text search across OneNote and return matching pages. |
| `onenote_get_page` | Read a page. as_text=True flattens the XML to plain text. |
| `onenote_hierarchy` | List notebooks / section groups / sections / pages. |
| `onenote_navigate` | Bring a page/section into view in the OneNote window. |
| `onenote_update_page_xml` | Escape hatch: push raw OneNote page XML through UpdatePageContent. |

## `access_*` - ADO queries and macros against .accdb/.mdb

| tool | what it does |
| --- | --- |
| `access_execute` | Run a data-modifying statement (INSERT/UPDATE/DELETE/DDL). Needs confirm=True. |
| `access_export` | Export a table or SELECT result to .csv (UTF-8 with BOM for Excel). |
| `access_list_objects` | List tables, views and their column definitions in an .accdb/.mdb file. |
| `access_query` | Run a SELECT (or a saved query name) and return rows. |
| `access_run_macro` | Open the database in Access and run a macro or VBA procedure. |

## `publisher_*` - publications: read text, export PDF

| tool | what it does |
| --- | --- |
| `publisher_close` | Close a publication. |
| `publisher_export_pdf` | Export a publication to PDF. |
| `publisher_open` | Open a .pub publication. |
| `publisher_read_text` | Extract the text of every story in a publication. |

## `com_*` - generic COM: describe / get / set / call / eval on any object

| tool | what it does |
| --- | --- |
| `com_call` | Call any method by path with positional/keyword arguments. |
| `com_describe` | Introspect a live COM object: its methods and properties. |
| `com_dispatch` | Talk to ANY COM server on this machine, not just Office. |
| `com_eval` | Escape hatch: evaluate a Python expression against live COM objects. |
| `com_get` | Read any property by path, e.g. path="ActiveSheet.UsedRange.Address()". |
| `com_set` | Set any property by path, e.g. path="ActiveSheet.Range('A1').Value". |

## `notes_*` - read notes of any format (md / txt / docx / pdf) into cited lines

| tool | what it does |
| --- | --- |
| `notes_read` | Read notes of any supported format into numbered lines. |

## `pdf_*` - extract text from PDFs

| tool | what it does |
| --- | --- |
| `pdf_read_text` | Extract text from a PDF (research note, filing, transcript). |

## `office_*` - cross-app file conversion

| tool | what it does |
| --- | --- |
| `office_convert` | Convert an Office file to PDF (or another format) by round-tripping COM. |
